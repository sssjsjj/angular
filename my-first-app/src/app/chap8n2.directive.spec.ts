import { Chap8n2Directive } from './chap8n2.directive';

describe('Chap8n2Directive', () => {
  it('should create an instance', () => {
    const directive = new Chap8n2Directive();
    expect(directive).toBeTruthy();
  });
});
