import { NumCheckDirective } from './num-check.directive';

describe('NumCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new NumCheckDirective();
    expect(directive).toBeTruthy();
  });
});
